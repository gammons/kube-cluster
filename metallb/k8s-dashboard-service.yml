apiVersion: v1
kind: Service
metadata:
  name: kubernetes-ip-service
  namespace: kubernetes-dashboard
  annotations:
    metallb.universe.tf/address-pool: default
spec:
  ports:
  - port: 80
    targetPort: 8001
  selector:
    k8s-app: kubernetes-dashboard
  type: LoadBalancer
