---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: immich-takeout
  namespace: immich
spec:
  storageClassName: nfs-bulk
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1500Gi
